import './App.css';
import Pin from './component/Pin';

function App() {
  return (
    <div className="App">
      React App
      <Pin length={5} maxLength={1} />
    </div>
  );
}

export default App;
